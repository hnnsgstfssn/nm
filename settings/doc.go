// Package settings binds org.freedesktop.NetworkManager.Settings, the
// profile store.
//
// It lists, looks up and adds saved profiles; changing or deleting one is
// [github.com/hnnsgstfssn/nm/connection], which addresses the profile itself. Adding a
// profile does not activate it, so onboarding is two steps: add here, then
// activate through [github.com/hnnsgstfssn/nm/manager].
package settings
